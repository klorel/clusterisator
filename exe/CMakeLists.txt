
IF(FALSE)
ADD_EXECUTABLE( unipartite  main_unipartite.cpp )
IF( WIN32 )
	TARGET_LINK_LIBRARIES(unipartite dataunipartite columnsgeneration clustering heuristic ${CPLEX_LIB}/ilocplex.lib ${CPLEX_LIB}/cplex124.lib ) 
ELSE()
	TARGET_LINK_LIBRARIES(unipartite dataunipartite columnsgeneration clustering heuristic ${CPLEX_LIB}/libilocplex.a ${CPLEX_LIB}/libcplex.a pthread ) 
ENDIF()
ENDIF()
ADD_EXECUTABLE( bipartite  main_bipartite.cpp )
IF( WIN32 )
	TARGET_LINK_LIBRARIES(bipartite databipartite columnsgeneration clustering heuristic ${CPLEX_LIB}/ilocplex.lib ${CPLEX_LIB}/cplex124.lib ) 
ELSE()
	TARGET_LINK_LIBRARIES(bipartite databipartite columnsgeneration clustering  heuristic ${CPLEX_LIB}/libilocplex.a ${CPLEX_LIB}/libcplex.a pthread ) 
ENDIF()
